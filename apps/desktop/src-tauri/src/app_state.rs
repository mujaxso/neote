// App state placeholder for future use
#[derive(Default)]
pub struct AppState {
    // Will be used for additional app state in the future
}

impl AppState {
    pub fn new() -> Self {
        Self::default()
    }
}
