//! Command-line tool to download and install Tree-sitter grammars.
//! This is kept for backward compatibility, but grammars are now auto-installed.

fn main() {
    println!("Grammars are now auto-installed when the application starts.");
    println!("If you need to manually install grammars, modify the app initialization.");
}
